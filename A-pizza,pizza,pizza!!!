#include<stdio.h>
int main()
{
	long long int t,n;
	scanf("%lld",&n);
	t=n+1;
	if(n==1 || n==0)
	{
		printf("%lld",n);
	}
	else if(t%2==0)
	{
		printf("%lld",t/2);
	}
	else
	{
		printf("%lld",t);
	}
	return 0;
}
